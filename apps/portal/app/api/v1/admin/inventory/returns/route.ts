import { stockReturnQuerySchema } from "@germinatura/contracts";
import { createRequestId } from "@germinatura/observability";
import { NextResponse } from "next/server";
import { AuthorizationError, requirePermission } from "@/lib/auth";
import { createAuthenticatedSupabaseClient } from "@/lib/authenticated-supabase";
import { mapContext, returnError, returnFail, returnHeaders } from "../../../inventory/returns/helpers";
export async function GET(request:Request){const requestId=createRequestId(request.headers);try{await requirePermission("inventory.manage");const parsed=stockReturnQuerySchema.safeParse(Object.fromEntries(new URL(request.url).searchParams));if(!parsed.success)return returnFail(requestId,"INVALID_RETURN_QUERY","Consulta de devoluções inválida.",422);const client=await createAuthenticatedSupabaseClient(request);const{data,error}=await client.rpc("get_stock_returns",{p_cursor:parsed.data.cursor??null,p_limit:parsed.data.limit});if(error)return returnError(requestId,error);const result=mapContext(data,requestId);if(!result.success)return returnFail(requestId,"INVENTORY_UNAVAILABLE","A consulta retornou dados inválidos.",503);return NextResponse.json(result.data,{headers:returnHeaders(requestId)});}catch(error){if(error instanceof AuthorizationError)return returnFail(requestId,error.status===401?"UNAUTHENTICATED":"FORBIDDEN",error.message,error.status);return returnFail(requestId,"INVENTORY_UNAVAILABLE","Não foi possível carregar as devoluções.",503);}}
